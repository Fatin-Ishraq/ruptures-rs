# ruptures-rs

Fast, drop-in change point detection for Python, powered by Rust.

Replace `import ruptures as rpt` with `import ruptures_rs as rpt`. Nothing else
changes: same classes, same arguments, same breakpoints.

Benchmarks and full documentation below — see `bench/` and `tests/`.
